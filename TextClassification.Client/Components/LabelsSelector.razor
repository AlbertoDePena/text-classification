@inherits LabelsSelectorBase

<div class="panel">
    <div class="header">
        Labels
    </div>
    <div class="content">
        <input class="filter-label-input" placeholder="Filter labels..." @oninput=OnFilterLabelChanged />
        <div class="labels">
            @if (FilteredLabels != null)
            {
                @foreach (var label in FilteredLabels)
                {
                    <div class="label" @key=label.Id @onclick="(() => OnLabelSelectedAsync(label))">@label.Name</div>
                }
            }
        </div>
    </div>
</div>